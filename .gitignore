.venv/
__pycache__/
*.pyc
*.egg-info/
.eggs/
dist/
build/
*.ico
*.pyd
*.so
.forge/
report/
data.txt
stress_results.json
.mypy_cache/
.pytest_cache/
htmlcov/

# ── Secrets — never commit these ──
forge_origin.key          # Ed25519 private key (lives in ~/.forge/, not here)
forge_origin.pub          # Public key is embedded in passport.py
*.passport.json           # Issued passport files
.env
.env.*
*.pem
*.p12
*.pfx
*.key
secrets.yaml
secrets.json

# Server — deployed separately, never in the public repo
server/

# Scratch/junk files
report.md
report_section_*.txt
